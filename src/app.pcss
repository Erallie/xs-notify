@tailwind base;
@tailwind components;
@tailwind utilities;



button>svg {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 50%;
    height: 50%;
}

button:has(>svg) {
    position: relative;
}

#about-links>a>svg {
    display: inline;
    width: 1.5rem;
    height: 1.5rem;
    margin-right: 0.5rem;
    vertical-align: middle;
    transform: translate(0, -5%);
    /* opacity: 80%; */
}

#console-el {
    max-height: 68dvh;
}
#bridge-button {
    width: 80%;
}
